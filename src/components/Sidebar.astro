---
/**
 * Override the Sidebar to render only Starlight's default sidebar, suppressing the
 * in-sidebar topic list that `starlight-sidebar-topics` renders by default. The topic
 * switcher is rendered in the header instead (see SiteTitle.astro). The current topic's
 * sidebar items are still set by the plugin's route middleware.
 */
import Default from '@astrojs/starlight/components/Sidebar.astro';
---

<Default><slot /></Default>
